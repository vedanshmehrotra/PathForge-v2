/** @type {import('next').NextConfig} */
const nextConfig = {
  // PathForge is a standalone frontend — all computation happens on the backend.
  // No rewrites needed; API calls go directly to NEXT_PUBLIC_API_URL.
}

export default nextConfig
